// post.applications(data) -> { method: 'POST', url: '/applications', body: data }
// post.deviceNetworkTypeLinks(data) -> { method: 'POST', url: '/device-network-type-links', body: data }

// load
// API.devices.load(id) -> { method: 'GET', url: `devices/${id}` }

// list
// API.devices.list(params) -> { method: 'GET', url: `/devices`, params }

// create
// API.devices.create(body)

// start application
// API.applications.start
